{% if content %}
  <main{{ attributes.addClass(['region', 'region-content']).setAttribute('role', 'main') }}>
    <a id="main-content" tabindex="-1"></a>{# link is in html.html.twig #}
    {{ content }}
  </main>
{% endif %}
